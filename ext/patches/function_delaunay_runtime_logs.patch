--- a/ext/function_delaunay/include/function_delaunay/Meb_accessors.h
+++ b/ext/function_delaunay/include/function_delaunay/Meb_accessors.h
@@ -20,6 +20,8 @@
 
 #pragma once
 
+#include <function_delaunay/log_utils.h>
+
 #include <function_delaunay/Miniball.hpp>
 
 #include <CGAL/Min_sphere_of_spheres_d_traits_d.h>
@@ -103,9 +105,9 @@
 	/*
 	  for(auto it = meb.support_begin();it!=meb.support_end();it++) {
 	  for(int i =0;i<DIM;i++) {
-	  std::cout << ((*it).first)[i] << " ";
-	  }
-	  std::cout << std::endl;
+	  FUNCTION_DELAUNAY_COUT << ((*it).first)[i] << " ";
+	  }
+	  FUNCTION_DELAUNAY_COUT << std::endl;
 	  }
 	*/
 
@@ -156,9 +158,9 @@
 	/*
 	  for(auto it = meb.support_begin();it!=meb.support_end();it++) {
 	  for(int i =0;i<DIM;i++) {
-	  std::cout << ((*it).first)[i] << " ";
-	  }
-	  std::cout << std::endl;
+	  FUNCTION_DELAUNAY_COUT << ((*it).first)[i] << " ";
+	  }
+	  FUNCTION_DELAUNAY_COUT << std::endl;
 	  }
 	*/
 	
@@ -226,16 +228,16 @@
 	/*
 	  for(Point p : points) {
 	  for(int i=0;i<dim;i++) {
-	  std::cout << p[i] << " ";
-	  }
-	  std::cout << std::endl;
+	  FUNCTION_DELAUNAY_COUT << p[i] << " ";
+	  }
+	  FUNCTION_DELAUNAY_COUT << std::endl;
 	  }
 	  
 	  if(! contains_circumcenter(points.begin(),points.end()) ) {
-	  std::cout << "Does not contain center" << std::endl;
+	  FUNCTION_DELAUNAY_COUT << "Does not contain center" << std::endl;
 	  }
 	  if(! is_enclosing(points.begin(),points.end())) {
-	  std::cout << "not enclosing" << std::endl;
+	  FUNCTION_DELAUNAY_COUT << "not enclosing" << std::endl;
 	  }
 	*/
 	return contains_circumcenter(points.begin(),points.end()) && is_enclosing(points.begin(),points.end());
@@ -243,7 +245,7 @@
       
       int update_at(std::vector<int>& indices,int pos,std::vector<Point>& points) {
 	if(pos==-1) {
-	  std::cout << "SOMETHING IS WRONG, I SHOULD NOT BE HERE" << std::endl;
+	  FUNCTION_DELAUNAY_COUT << "SOMETHING IS WRONG, I SHOULD NOT BE HERE" << std::endl;
 	  return -1;
 	}
 	indices[pos]++;
@@ -327,11 +329,11 @@
 	double result;
 	while(true) {
 	  /*
-	    std::cout << "At pos ";
+	    FUNCTION_DELAUNAY_COUT << "At pos ";
 	    for(int i=0;i<curr_indices.size();i++) {
-	    std::cout << curr_indices[i] << " ";
+	    FUNCTION_DELAUNAY_COUT << curr_indices[i] << " ";
 	    }
-	    std::cout << std::endl;
+	    FUNCTION_DELAUNAY_COUT << std::endl;
 	  */
 	  if(check(curr_points)) {
 	    return sqrt(CGAL::to_double(sq_radius(curr_points.begin(),curr_points.end())));
--- a/ext/function_delaunay/include/function_delaunay/boost_timers.h
+++ b/ext/function_delaunay/include/function_delaunay/boost_timers.h
@@ -19,6 +19,8 @@
 
 
 #pragma once
+
+#include <function_delaunay/log_utils.h>
 
 #include <iostream>
 #include <boost/timer/timer.hpp>
@@ -61,29 +63,29 @@
 
 void pretty_print_timer(boost::timer::cpu_timer& timer,double total) {
   if(double(timer.elapsed().wall)/std::pow(10,9)<0.0001) {
-    std::cout << "0" << std::endl;
+    FUNCTION_DELAUNAY_COUT << "0" << std::endl;
   } else{
-    std::cout << double(timer.elapsed().wall)/std::pow(10,9) << "     ( "  <<  double(timer.elapsed().wall)/total*100 << "% )" << std::endl;
+    FUNCTION_DELAUNAY_COUT << double(timer.elapsed().wall)/std::pow(10,9) << "     ( "  <<  double(timer.elapsed().wall)/total*100 << "% )" << std::endl;
   }
 }
 
 void print_timers(double total=double(overall_timer.elapsed().wall),bool print_overall=true) {
   if(print_overall) {
-    std::cout << "Overall timer: " << double(overall_timer.elapsed().wall)/std::pow(10,9) << std::endl;
+    FUNCTION_DELAUNAY_COUT << "Overall timer: " << double(overall_timer.elapsed().wall)/std::pow(10,9) << std::endl;
   }
-  std::cout << "Inital timer:           "; pretty_print_timer(initial_timer,total);
-  std::cout << "Complex timer:          "; pretty_print_timer(complex_timer,total);
-  std::cout << "Face timer:             "; pretty_print_timer(face_timer,total);
-  std::cout << "Meb timer:              "; pretty_print_timer(meb_timer,total);
-  std::cout << "Graded matrices timer:  "; pretty_print_timer(graded_matrices_timer,total);
-  std::cout << "Multi chunk timer:      "; pretty_print_timer(multi_chunk_timer,total);
-  std::cout << "Mpfree timer:           "; pretty_print_timer(mpfree_timer,total);
-  std::cout << "File output timer:      "; pretty_print_timer(file_output_timer,total);
-  std::cout << "Delaunay timer:         "; pretty_print_timer(delaunay_timer,total);
-  std::cout << "Test timer 1:           "; pretty_print_timer(test_timer_1,total);
-  std::cout << "Test timer 2:           "; pretty_print_timer(test_timer_2,total);
-  std::cout << "Test timer 3:           "; pretty_print_timer(test_timer_3,total);
-  std::cout << "Test timer 4:           "; pretty_print_timer(test_timer_4,total);
+  FUNCTION_DELAUNAY_COUT << "Inital timer:           "; pretty_print_timer(initial_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Complex timer:          "; pretty_print_timer(complex_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Face timer:             "; pretty_print_timer(face_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Meb timer:              "; pretty_print_timer(meb_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Graded matrices timer:  "; pretty_print_timer(graded_matrices_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Multi chunk timer:      "; pretty_print_timer(multi_chunk_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Mpfree timer:           "; pretty_print_timer(mpfree_timer,total);
+  FUNCTION_DELAUNAY_COUT << "File output timer:      "; pretty_print_timer(file_output_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Delaunay timer:         "; pretty_print_timer(delaunay_timer,total);
+  FUNCTION_DELAUNAY_COUT << "Test timer 1:           "; pretty_print_timer(test_timer_1,total);
+  FUNCTION_DELAUNAY_COUT << "Test timer 2:           "; pretty_print_timer(test_timer_2,total);
+  FUNCTION_DELAUNAY_COUT << "Test timer 3:           "; pretty_print_timer(test_timer_3,total);
+  FUNCTION_DELAUNAY_COUT << "Test timer 4:           "; pretty_print_timer(test_timer_4,total);
 }
 
   
--- a/ext/function_delaunay/include/function_delaunay/compute_meb_radii_of_simplex_tree.h
+++ b/ext/function_delaunay/include/function_delaunay/compute_meb_radii_of_simplex_tree.h
@@ -20,6 +20,8 @@
 
 #pragma once
 
+#include <function_delaunay/log_utils.h>
+
 #include <function_delaunay/Meb_accessors.h>
 #include <CGAL/Epick_d.h>
 
@@ -41,18 +43,18 @@
 												  std::vector<int> &lower,
 												  double meb_radius) {
 	/*
-	  std::cout << "Start recursion: \n";
+	  FUNCTION_DELAUNAY_COUT << "Start recursion: \n";
 	  auto vertices = simplex_tree.simplex_vertex_range(upper);
-	  std::cout << "Upper: ";
+	  FUNCTION_DELAUNAY_COUT << "Upper: ";
 	  for(auto vit=vertices.begin();vit!=vertices.end();vit++) {
-	  std::cout << *vit << " ";
+	  FUNCTION_DELAUNAY_COUT << *vit << " ";
 	  }
-	  std::cout << std::endl;
-	  std::cout << "Lower: ";
+	  FUNCTION_DELAUNAY_COUT << std::endl;
+	  FUNCTION_DELAUNAY_COUT << "Lower: ";
 	  for(auto vit=lower.begin();vit!=lower.end();vit++) {
-	  std::cout << *vit << " ";
+	  FUNCTION_DELAUNAY_COUT << *vit << " ";
 	  }
-	  std::cout << std::endl;
+	  FUNCTION_DELAUNAY_COUT << std::endl;
 	*/
 	auto bd = simplex_tree.boundary_opposite_vertex_simplex_range(upper);
 	for(auto bd_it=bd.begin();bd_it!=bd.end();bd_it++) {
@@ -67,11 +69,11 @@
 	
 		auto vertices = simplex_tree.simplex_vertex_range(bd_simp);
 		/*
-		  std::cout << "Assigningfilt val to: ";
+		  FUNCTION_DELAUNAY_COUT << "Assigningfilt val to: ";
 		  for(auto vit=vertices.begin();vit!=vertices.end();vit++) {
-		  std::cout << *vit << " ";
+		  FUNCTION_DELAUNAY_COUT << *vit << " ";
 		  }
-		  std::cout << std::endl;
+		  FUNCTION_DELAUNAY_COUT << std::endl;
 		*/
 		simplex_tree.assign_filtration(bd_simp,meb_radius);
 		_assign_filtration_values_in_interval(simplex_tree,bd_simp,lower,meb_radius);
@@ -80,11 +82,11 @@
     
 	/*
 	  auto vertices = simplex_tree.simplex_vertex_range(curr_pair.first);
-	  std::cout << "Pair first: ";
+	  FUNCTION_DELAUNAY_COUT << "Pair first: ";
 	  for(auto vit=vertices.begin();vit!=vertices.end();vit++) {
-	  std::cout << *vit << " ";
+	  FUNCTION_DELAUNAY_COUT << *vit << " ";
 	  }
-	  std::cout << "\nPair second: " << curr_pair.second << std::endl;
+	  FUNCTION_DELAUNAY_COUT << "\nPair second: " << curr_pair.second << std::endl;
 	*/
     
     }
@@ -147,7 +149,7 @@
 		    } 
 		    /*
 		      else {
-		      std::cout << "Radius already known, not taking the computed radius" << std::endl;
+		      FUNCTION_DELAUNAY_COUT << "Radius already known, not taking the computed radius" << std::endl;
 		      }
 		    */
 		    simplex_tree.assign_filtration(*it,meb_radius);
@@ -159,7 +161,7 @@
 		}
 	    }
 	}
-	std::cout << "Computed " << number_of_meb_computations << " mebs (number of simplices=" << simplex_tree.num_simplices() << ")" << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Computed " << number_of_meb_computations << " mebs (number of simplices=" << simplex_tree.num_simplices() << ")" << std::endl;
 
 #else // the naive "go over all simplices and compute meb solution
 	auto all_simplices = simplex_tree.complex_simplex_range();
@@ -184,8 +186,8 @@
 	}
 #endif
 #if !NDEBUG
-	auto former_precision = std::cout.precision();
-	std::cout.precision(std::numeric_limits<double>::max_digits10);
+	auto former_precision = FUNCTION_DELAUNAY_COUT.precision();
+	FUNCTION_DELAUNAY_COUT.precision(std::numeric_limits<double>::max_digits10);
 
 	for(auto it=all_simplices.begin();it!=all_simplices.end();it++) {
       
@@ -198,30 +200,30 @@
 		    bool bad_order = filt_value<filt_bd;
 		    bool very_close_unequal = filt_value>filt_bd && (filt_value-filt_bd<0.000000000001);
 		    if(bad_order) {
-			std::cout << "Found bad pair" << std::endl;
+			FUNCTION_DELAUNAY_COUT << "Found bad pair" << std::endl;
 		    }
 		    if(very_close_unequal) {
-			std::cout << "Found very close pair" << std::endl;
+			FUNCTION_DELAUNAY_COUT << "Found very close pair" << std::endl;
 		    }
 		    if(bad_order || very_close_unequal) {
 	    
 			auto vertices = simplex_tree.simplex_vertex_range(*it);
-			std::cout << "Simplex: ";
+			FUNCTION_DELAUNAY_COUT << "Simplex: ";
 			for(auto vit=vertices.begin();vit!=vertices.end();vit++) {
-			    std::cout << *vit << " ";
+			    FUNCTION_DELAUNAY_COUT << *vit << " ";
 			}
-			std::cout << ", value: " << filt_value << std::endl;
-			std::cout << "Face: ";
+			FUNCTION_DELAUNAY_COUT << ", value: " << filt_value << std::endl;
+			FUNCTION_DELAUNAY_COUT << "Face: ";
 			vertices = simplex_tree.simplex_vertex_range(*bd_it);
 			for(auto vit=vertices.begin();vit!=vertices.end();vit++) {
-			    std::cout << *vit << " ";
+			    FUNCTION_DELAUNAY_COUT << *vit << " ";
 			}
-			std::cout << ", value: " << filt_bd << std::endl;
+			FUNCTION_DELAUNAY_COUT << ", value: " << filt_bd << std::endl;
 		    }
 		}
 	    }
 	}
-	std::cout.precision(former_precision);
+	FUNCTION_DELAUNAY_COUT.precision(former_precision);
 #endif
 
     }
@@ -251,7 +253,7 @@
 	    meb2.new_meb(points.begin(), points.end());
 	    double meb_radius2 = meb2.get_radius();
 	    if(std::abs(meb_radius1-meb_radius2)>0.0000001) {
-		std::cout << "Discrepancy: " << std::abs(meb_radius1-meb_radius2) << std::endl;
+		FUNCTION_DELAUNAY_COUT << "Discrepancy: " << std::abs(meb_radius1-meb_radius2) << std::endl;
 	    }
 	    simplex_tree.assign_filtration(*it,meb_radius1);
 	}
--- a/ext/function_delaunay/include/function_delaunay/count_size_of_delaunay_triangulation.h
+++ b/ext/function_delaunay/include/function_delaunay/count_size_of_delaunay_triangulation.h
@@ -19,6 +19,8 @@
 
 
 #pragma once
+
+#include <function_delaunay/log_utils.h>
 
 #include <function_delaunay/Delaunay_triangulation_accessors.h>
 
@@ -59,7 +61,7 @@
 	test_timer_1.stop();
 #endif
     
-	std::cout  << "Delaunay computed, now counting simplices" << std::endl;
+	FUNCTION_DELAUNAY_COUT  << "Delaunay computed, now counting simplices" << std::endl;
 
 	// Compute the size of the Delaunay complex (via simplex tree, as the functionality is not in CGAL)
 	int counter=0;
@@ -108,13 +110,13 @@
 
 	if(d==2) {
 	    // Delaunay_triangulation_2 seems faster than the general one
-	    std::cout << "In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
 	    typedef Delaunay_triangulation_accessor_2 DT_accessor;
 	    DT_accessor T;
 	    number_of_simplices= _count_size_of_delaunay_triangulation(T,input_points,number_of_k_simplices);
 	} else if(d==3) {
 	    // Same for Delaunay triangulation_3
-	    std::cout << "In space, using CGAL::Delaunay_triangulation_3" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "In space, using CGAL::Delaunay_triangulation_3" << std::endl;
 	    typedef Delaunay_triangulation_accessor_3 DT_accessor;
 	    DT_accessor T;
 	    number_of_simplices= _count_size_of_delaunay_triangulation(T,input_points,number_of_k_simplices);
--- a/ext/function_delaunay/include/function_delaunay/function_delaunay_with_meb.h
+++ b/ext/function_delaunay/include/function_delaunay/function_delaunay_with_meb.h
@@ -19,6 +19,8 @@
 
 
 #pragma once
+
+#include <function_delaunay/log_utils.h>
 
 #include <mpp_utils/create_graded_matrices_from_simplex_tree.h>
 #include <gudhi/Simplex_tree.h>
@@ -76,7 +78,7 @@
 
 	typedef Gudhi::Simplex_tree<> Simplex_tree;
 	
-	std::cout << "Start complex" << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Start complex" << std::endl;
     
 	// The simplices are stored just by boundary vertex indices first
 	// The filtration values and the order are computed later!
@@ -90,11 +92,11 @@
 	complex_timer.stop();
 #endif
     
-	std::cout << "Collected " << simplices.size() << " simplices" << std::endl;
-
-    
-#if WITH_MEMORY_PROFILE
-	std::cout << "Memory after complex: " << mem_info() << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Collected " << simplices.size() << " simplices" << std::endl;
+
+    
+#if WITH_MEMORY_PROFILE
+	FUNCTION_DELAUNAY_COUT << "Memory after complex: " << mem_info() << std::endl;
 #endif
 
     
@@ -111,10 +113,10 @@
     
 	long total_number_of_simplices = simplex_tree.num_simplices();
     
-	std::cout << "Simplex tree has " << simplex_tree.num_vertices() << " vertices and " << total_number_of_simplices << " simplices" << std::endl;
-    
-#if WITH_MEMORY_PROFILE
-	std::cout << "Memory after face: " << mem_info() << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Simplex tree has " << simplex_tree.num_vertices() << " vertices and " << total_number_of_simplices << " simplices" << std::endl;
+    
+#if WITH_MEMORY_PROFILE
+	FUNCTION_DELAUNAY_COUT << "Memory after face: " << mem_info() << std::endl;
 #endif
     
 	if(only_return_complex_size) {
@@ -128,7 +130,7 @@
 
 	compute_meb_radii_of_simplex_tree(simplex_tree,input_points);
 
-	std::cout << "Computed all meb values, now sorting" << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Computed all meb values, now sorting" << std::endl;
 
 #if 1 || !NDEBUG || !FUNCTION_DELAUNAY_SMART_MEB_TRAVERSAL
 
@@ -140,10 +142,10 @@
 	double adjustment = adjust_simplex_tree(simplex_tree);
 	
 	if(adjustment>0) {
-	    std::cout << "Some meb value had to be adjusted to fit its cofacet" << std::endl;
-	    std::cout << "Maximal adjustment: " << adjustment << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "Some meb value had to be adjusted to fit its cofacet" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "Maximal adjustment: " << adjustment << std::endl;
 	} else {
-	    std::cout << "No adjustment of meb values was needed" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "No adjustment of meb values was needed" << std::endl;
 	}
     
 #if FUNCTION_DELAUNAY_SMART_MEB_TRAVERSAL
@@ -159,7 +161,7 @@
 #endif
     
 #if WITH_MEMORY_PROFILE
-	std::cout << "Memory after bigrade: " << mem_info() << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Memory after bigrade: " << mem_info() << std::endl;
 #endif
     }
 	
@@ -187,7 +189,7 @@
 	    return total_number_of_simplices;
 	}
     
-	std::cout << "Building graded boundary matrices" << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Building graded boundary matrices" << std::endl;
     
 #if FUNCTION_DELAUNAY_TIMERS
 	graded_matrices_timer.start();
@@ -205,7 +207,7 @@
 	simplex_tree.prune_above_dimension(-1);
   
 	for(int i=0;i<=simplex_tree_dimension;i++) {
-	    std::cout << "Simplices in dimension " << i << ": " << graded_matrices[simplex_tree_dimension-i].get_num_cols() << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "Simplices in dimension " << i << ": " << graded_matrices[simplex_tree_dimension-i].get_num_cols() << std::endl;
 	}
     
 #if FUNCTION_DELAUNAY_TIMERS
@@ -213,7 +215,7 @@
 #endif
 
 #if WITH_MEMORY_PROFILE
-	std::cout << "Memory after boundary: " << mem_info() << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Memory after boundary: " << mem_info() << std::endl;
 #endif
   
 	return total_number_of_simplices;
--- a/ext/function_delaunay/include/function_delaunay/get_simplices_from_triangulation.h
+++ b/ext/function_delaunay/include/function_delaunay/get_simplices_from_triangulation.h
@@ -20,6 +20,8 @@
 
 #pragma once
 
+#include <function_delaunay/log_utils.h>
+
 #include <boost/container_hash/hash.hpp>
 
 typedef std::unordered_set<
@@ -50,12 +52,12 @@
 #ifdef DBG_PRINT
 #define PRINT_SIMPLICES(simplices)                                       \
     do {                                                                 \
-        std::cout << "Debug: List of simplices at the end of procedure:" << std::endl; \
+        FUNCTION_DELAUNAY_COUT << "Debug: List of simplices at the end of procedure:" << std::endl; \
         for (const auto& s : simplices) {                                \
-            for (int v : s) std::cout << v << " ";                       \
-            std::cout << std::endl;                                      \
+            for (int v : s) FUNCTION_DELAUNAY_COUT << v << " ";                       \
+            FUNCTION_DELAUNAY_COUT << std::endl;                                      \
         }                                                                \
-        std::cout << "End of final list" << std::endl;                   \
+        FUNCTION_DELAUNAY_COUT << "End of final list" << std::endl;                   \
     } while (0)
 #else
 #define PRINT_SIMPLICES(simplices) do {} while (0)
@@ -119,11 +121,11 @@
 		    simplices.push_back(cell_idx);
 	  
 		    /*
-		      std::cout << "Cell: ";
+		      FUNCTION_DELAUNAY_COUT << "Cell: ";
 		      for(int x: cell_idx) {
-		      std::cout <<  x << " ";
+		      FUNCTION_DELAUNAY_COUT <<  x << " ";
 		      }
-		      std::cout << std::endl;
+		      FUNCTION_DELAUNAY_COUT << std::endl;
 		    */
 		}
 		vh = T.insert(p,loc);
@@ -137,7 +139,7 @@
 	    counter++;
       
 	}
-	std::cout << "Number of insertions outside convex hull " << no_points_outside_convex_hull << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Number of insertions outside convex hull " << no_points_outside_convex_hull << std::endl;
     }
 
     void get_simplices_from_triangulation_one_function(std::vector<Point_with_densities> &input_points,
@@ -150,13 +152,13 @@
 
 	if(d==2) {
 	    // Delaunay_triangulation_2 seems faster than the general one
-	    std::cout << "In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
 	    typedef Delaunay_triangulation_accessor_2 DT_accessor;
 	    DT_accessor T;
 	    _get_simplices_from_triangulation_one_function(T,input_points,simplices);
 	} else if(d==3) {
 	    // Same for Delaunay triangulation_3
-	    std::cout << "In space, using CGAL::Delaunay_triangulation_3" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "In space, using CGAL::Delaunay_triangulation_3" << std::endl;
 	    typedef Delaunay_triangulation_accessor_3 DT_accessor;
 	    DT_accessor T;
 	    _get_simplices_from_triangulation_one_function(T,input_points,simplices);
@@ -231,11 +233,11 @@
 
 	    }
 	    /*
-	      std::cout << "Cell: ";
+	      FUNCTION_DELAUNAY_COUT << "Cell: ";
 	      for(int x: cell_idx) {
-	      std::cout <<  x << " ";
+	      FUNCTION_DELAUNAY_COUT <<  x << " ";
 	      }
-	      std::cout << std::endl;
+	      FUNCTION_DELAUNAY_COUT << std::endl;
 	    */
 	    vh_new = T.insert(p,loc);
 	    T.data_of_vertex(vh_new).idx = point.idx;
@@ -244,7 +246,7 @@
 	    //std::cout << "Inserting " << point.idx << ", coors " << point.x[0] << " " << point.x[1] << "..." << std::endl;
 	    /*
 	      for(auto it = T.vertices_begin(); it!=T.vertices_end();it++) {
-	      std::cout << "Present is vertex " << T.data_of_vertex(it).idx << std::endl;
+	      FUNCTION_DELAUNAY_COUT << "Present is vertex " << T.data_of_vertex(it).idx << std::endl;
 	      }
 	    */
 
@@ -281,22 +283,22 @@
 	//std::cout << "Conflict sizes: "  << conflicts1.size() << " " << conflicts2.size() << std::endl;
         
 #ifdef DBG_PRINT
-	std::cout << "Conflict1:" << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Conflict1:" << std::endl;
 
 	for(auto v : conflicts1) {
 	    for (int x : v) {
-		std::cout << x << " ";
+		FUNCTION_DELAUNAY_COUT << x << " ";
 	    }
-	    std::cout << std::endl;
-	}
-
-	std::cout << "Conflict2:" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << std::endl;
+	}
+
+	FUNCTION_DELAUNAY_COUT << "Conflict2:" << std::endl;
 
 	for(auto v : conflicts2) {
 	    for (int x : v) {
-		std::cout << x << " ";
+		FUNCTION_DELAUNAY_COUT << x << " ";
 	    }
-	    std::cout << std::endl;
+	    FUNCTION_DELAUNAY_COUT << std::endl;
 	}
 #endif
 	// Quick and dirty solution: Create a std::set with the conflicts1, and just search each element in conflicts2:
@@ -307,11 +309,11 @@
 	for(auto s : conflicts2) {
 	    if(conflict_map1.count(s)) {
 		/*
-		std::cout << "FOUND DOUBLE CONFLICT: " << std::flush;
+		FUNCTION_DELAUNAY_COUT << "FOUND DOUBLE CONFLICT: " << std::flush;
 		for(int v : s) {
-		    std::cout << v << " ";
+		    FUNCTION_DELAUNAY_COUT << v << " ";
 		}
-		std::cout << std::endl;
+		FUNCTION_DELAUNAY_COUT << std::endl;
 		*/
 		std::vector<int> double_conflict_simplex;
 		std::copy(s.begin(),s.end(),std::back_inserter(double_conflict_simplex));
@@ -331,7 +333,7 @@
 							 std::vector<Point_with_densities> &input_points,
 							 Uset &simplices) {
 
-        std::cout << "Naive routine: \n";
+        FUNCTION_DELAUNAY_COUT << "Naive routine: \n";
 
 	int outside_convex_hull=0;
 
@@ -429,7 +431,7 @@
 
 		//std::cout << "Adding " << left.idx << "..." << std::flush;
 		add_point_to_triangulation(T,left,vh_left,conflicts_dummy,simplices,outside_convex_hull,false,true);
-		std::cout << "Simplices size at this step: " << simplices.size() << std::endl;
+		FUNCTION_DELAUNAY_COUT << "Simplices size at this step: " << simplices.size() << std::endl;
 
 		// Now remove them and re-insert them in opposite order
 		//std::cout << "Removing " << point.idx << std::endl;
@@ -458,7 +460,7 @@
 		find_double_conflicts(T,point.idx,left.idx,conflicts_up,conflicts_right,simplices);
 		test_timer_1.stop();
 
-                std::cout << "Simplices size at this step: " << simplices.size() << std::endl;
+                FUNCTION_DELAUNAY_COUT << "Simplices size at this step: " << simplices.size() << std::endl;
 		number_of_squares_visited++;
 		if(simplices.size()>old_number_of_simplices) {
 		    number_of_squares_needed++;
@@ -470,9 +472,9 @@
 	}
 
         PRINT_SIMPLICES(simplices);
-	std::cout << "Number of points: " << input_points.size() << std::endl;
-	std::cout << "Squares visited:  " << number_of_squares_visited << std::endl;
-	std::cout << "Squares needed:   " << number_of_squares_needed << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Number of points: " << input_points.size() << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Squares visited:  " << number_of_squares_visited << std::endl;
+	FUNCTION_DELAUNAY_COUT << "Squares needed:   " << number_of_squares_needed << std::endl;
     }
 
 
@@ -501,12 +503,12 @@
 
         // no null handle
         if (vh == Vertex_handle()) {
-            std::cerr << " idx_to_vh[" << point_idx << "] is nullptr.\n";
+            FUNCTION_DELAUNAY_CERR << " idx_to_vh[" << point_idx << "] is nullptr.\n";
             return higher;
         }
 
         if (T.is_infinite(vh)) {
-            std::cerr << " idx_to_vh[" << point_idx << "] is infinite.\n";
+            FUNCTION_DELAUNAY_CERR << " idx_to_vh[" << point_idx << "] is infinite.\n";
             return higher;
         }
 
@@ -547,7 +549,7 @@
         							 std::vector<Point_with_densities> &input_points,
         							 Uset &simplices) {
 
-            std::cout << "Calling ALT routine \n";
+            FUNCTION_DELAUNAY_COUT << "Calling ALT routine \n";
 
             typedef DelaunayTriangulationAccessor Delaunay_triangulation_accessor;
             typedef typename Delaunay_triangulation_accessor::Vertex_handle Vertex_handle;
@@ -675,9 +677,9 @@
 
             }
             PRINT_SIMPLICES(simplices);
-            std::cout << "Number of points: " << input_points.size() << std::endl;
-            std::cout << "Squares visited:  " << number_of_squares_visited << std::endl;
-            std::cout << "Squares needed:   " << number_of_squares_needed << std::endl;	
+            FUNCTION_DELAUNAY_COUT << "Number of points: " << input_points.size() << std::endl;
+            FUNCTION_DELAUNAY_COUT << "Squares visited:  " << number_of_squares_visited << std::endl;
+            FUNCTION_DELAUNAY_COUT << "Squares needed:   " << number_of_squares_needed << std::endl;	
         }
 
 
@@ -692,7 +694,7 @@
 	int d=input_points[0].dimension();
 
 	if(d==2) {
-	    std::cout << "(two functions) In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "(two functions) In the plane, using CGAL::Delaunay_triangulation_2" << std::endl;
 	    typedef Delaunay_triangulation_accessor_2 Delaunay_triangulation_accessor;
 	    Delaunay_triangulation_accessor T;
 #ifdef NAIVE
@@ -701,7 +703,7 @@
 	    _get_simplices_from_triangulation_two_functions(T,input_points,simplices);
 #endif 
 	} else if(d==3) {
-	    std::cout << "(two functions) In space, using CGAL::Delaunay_triangulation_3" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "(two functions) In space, using CGAL::Delaunay_triangulation_3" << std::endl;
       	    typedef Delaunay_triangulation_accessor_3 Delaunay_triangulation_accessor;
 	    Delaunay_triangulation_accessor T;
 	    //_get_simplices_from_triangulation_two_functions(T,input_points,simplices);
@@ -711,7 +713,7 @@
 	    _get_simplices_from_triangulation_two_functions(T,input_points,simplices);
 #endif 
 	} else {
-	    std::cout << "(two functions) In high dim, using CGAL::Delaunay_triangulation_d" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "(two functions) In high dim, using CGAL::Delaunay_triangulation_d" << std::endl;
 	    typedef CGAL::Dynamic_dimension_tag Dim_tag;
 	    typedef Delaunay_triangulation_accessor_d<Dim_tag> Delaunay_triangulation_accessor;
 	    Delaunay_triangulation_accessor T(d);
@@ -739,7 +741,7 @@
 
 	    std::copy(simplices_set.begin(),simplices_set.end(),std::back_inserter(simplices));
 	} else {
-	    std::cout << "Complex creation currently not implemented for " << number_of_functions << " number of functions" << std::endl;
+	    FUNCTION_DELAUNAY_COUT << "Complex creation currently not implemented for " << number_of_functions << " number of functions" << std::endl;
 	}
 	return;
     }
--- /dev/null
+++ b/ext/function_delaunay/include/function_delaunay/log_utils.h
@@ -0,0 +1,37 @@
+#pragma once
+
+#include <iostream>
+
+#include "ext_interface/backend_log_policy.hpp"
+
+namespace function_delaunay {
+
+class null_streambuf : public std::streambuf {
+ protected:
+  int overflow(int c) override { return traits_type::not_eof(c); }
+};
+
+inline std::ostream& log_stream() {
+  if (multipers::backend_log_policy::backend_log_enabled(
+          multipers::backend_log_policy::backend_log_bit::function_delaunay)) {
+    return std::cout;
+  }
+  static null_streambuf buffer;
+  static std::ostream out(&buffer);
+  return out;
+}
+
+inline std::ostream& error_stream() {
+  if (multipers::backend_log_policy::backend_log_enabled(
+          multipers::backend_log_policy::backend_log_bit::function_delaunay)) {
+    return std::cerr;
+  }
+  static null_streambuf buffer;
+  static std::ostream out(&buffer);
+  return out;
+}
+
+}  // namespace function_delaunay
+
+#define FUNCTION_DELAUNAY_COUT ::function_delaunay::log_stream()
+#define FUNCTION_DELAUNAY_CERR ::function_delaunay::error_stream()
--- a/ext/function_delaunay/mpfree_mod/include/mpfree/global.h
+++ b/ext/function_delaunay/mpfree_mod/include/mpfree/global.h
@@ -20,9 +20,18 @@
 
 #include<phat/helpers/misc.h>
 
+#if __has_include("ext_interface/backend_log_flag.hpp")
+#include "ext_interface/backend_log_flag.hpp"
+#endif
+
 namespace mpfree {
 
+
+#if __has_include("ext_interface/backend_log_flag.hpp")
+  inline multipers::backend_log_policy::runtime_flag<multipers::backend_log_policy::backend_log_bit::function_delaunay> verbose;
+#else
   bool verbose = false;
+#endif
 
   typedef phat::index index;
   
--- a/ext/function_delaunay/multi_chunk_mod/include/multi_chunk/basic.h
+++ b/ext/function_delaunay/multi_chunk_mod/include/multi_chunk/basic.h
@@ -20,10 +20,18 @@
 
 #include<mpp_utils/basic.h>
 
+#if __has_include("ext_interface/backend_log_flag.hpp")
+#include "ext_interface/backend_log_flag.hpp"
+#endif
+
 namespace multi_chunk {
 
   typedef mpp_utils::index index;
 
+#if __has_include("ext_interface/backend_log_flag.hpp")
+  inline multipers::backend_log_policy::runtime_flag<multipers::backend_log_policy::backend_log_bit::function_delaunay> verbose;
+#else
   bool verbose = true;
+#endif
 }
 
